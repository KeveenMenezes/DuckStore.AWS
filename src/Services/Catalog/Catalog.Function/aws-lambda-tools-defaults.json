{
  "Information": [
    "This file provides default values for the deployment wizard inside Visual Studio and the AWS Lambda commands added to the .NET Core CLI.",
    "To learn more about the Lambda commands with the .NET Core CLI execute the following command at the command line in the project root directory.",
    "dotnet lambda help",
    "All the command line options for the Lambda command can be specified in this file.",
    "function-runtime is provided.al2023 because net10.0 has no managed Lambda runtime. It is also what makes Amazon.Lambda.Annotations emit the ANNOTATIONS_HANDLER dispatching Main instead of handler classes (ADR-0042)."
  ],
  "profile": "",
  "region": "",
  "configuration": "Release",
  "package-type": "zip",
  "function-runtime": "provided.al2023",
  "function-architecture": "arm64",
  "msbuild-parameters": "--self-contained true"
}
